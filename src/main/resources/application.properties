spring.application.name=ecommerce-backend

# MongoDB Atlas
spring.data.mongodb.uri=mongodb+srv://guna:1234@cluster0.u6uuhwm.mongodb.net/ecommerce?retryWrites=true&w=majority

# Server port
server.port=${PORT:8080}

# CORS Configuration
# Provide comma-separated list of allowed origins from frontend
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# JWT Secret (change in production — must be >= 256 bits for HS256)
app.jwt.secret=${JWT_SECRET:LuxeCartSuperSecretKeyForJWT2024PleaseChangeInProduction!AbcDefGhiJkl}
app.jwt.expiration-ms=86400000

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_YOUR_KEY_ID}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:YOUR_KEY_SECRET}